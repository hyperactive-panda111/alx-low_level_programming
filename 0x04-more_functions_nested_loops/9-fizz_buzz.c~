#include <stdio.h>

/**
 * main - executing path
 * Return: 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 1; a < 101; ++a)
	{
		if (a % 3 == 0)
		{
			printf(" Fizz");
			
		}

		else if (a % 5 == 0)
		{
			printf(" Buzz");
		}

		else if ((a % 5 == 0) && (a % 3 == 0))
		{
			printf(" FizzBuzz");
		}
		else
			printf("%d ", a);
	}
	printf(10);
	return (0);
}
