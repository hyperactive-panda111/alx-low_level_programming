#include "main.h"

/**
 * print_triangle
 * @size: parameter
 * Return: void
 */
void print_triangle(int size)
{
	int a, b, c;

	for(a = 1; a <= size; a++)
	{
		for (b = size; b > i; b--)
		{
			_putchar(32);
		}
			for (c = 1; c <= b; c++)
			{
				_putchar(35)
			}
		_putchar(10);
		if (a != size)
			_putchar(10);
	}
}
