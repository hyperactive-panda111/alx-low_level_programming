#include <stdio.h>

/**
 * main - print natural numbers below 1024
 *
 * Return: Always 0
 */

int main(void)
{
	int a, b;

	for (a = 1; a < 1024; a++)
	{
		if ((a % 3) || (a % 5))
			b += a;
	}
	printf("%d\n", b);
	return (0);
}