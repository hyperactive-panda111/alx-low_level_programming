#include "main.h"

/**
 * _abs - print absolute value of input integer
 *
 * @n: parameter for input value
 *
 * Return: absolute value of integer
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
